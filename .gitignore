.build/
.swiftpm/
build/
*.xcodeproj/
DerivedData/
ClaudeUsage.app/
.DS_Store
.env

# Release artifacts (DMGs are published to GitHub Releases, not committed)
ClaudeUsage-v*.dmg
rw.*.dmg

# Original uncropped contributor photos (only the *-avatar.jpg crops are published)
docs/moffatt.jpg
docs/nowicki.jpeg

# Local Claude Code state (skills in .claude/skills ARE tracked)
.claude/settings.local.json
.claude/worktrees/

# Stray compiler outputs
*.d
*.o
*.swiftdeps

# Linux: folder-scoped Swift toolchain (see linux/build.sh)
.toolchain/
